# Create a Python script that utilizes at least 5 different string methods (e.g., upper(), lower(), split(), replace(), find()) to perform the following tasks:
# Read a given string from user input
# Convert the string to uppercase and lowercase
# Split the string into substrings based on a specified delimiter
# Replace a specific substring with another
# Find the index of a specified character or substring within the string
# Include example use cases for each method, demonstrating their functionality
# Ensure the script is well-structured, readable, and includes proper error handling for potential exceptions
# Submit the Python script as a .py file, along with a brief report explaining the purpose and functionality of each string method used.







string=input("enter a string")
print(string.upper())
print(string.lower())
print(string.split(" "))
print(string.replace("a","A"))
print(string.find("a"))


